MEMORY {
    HEADER: start = $1000, size = 2, file = %O;
    ROM: start = $1002, size = $7FFE, file = %O;
}

SEGMENTS {
    HEADER: load = HEADER, type = ro;
    CODE: load = ROM, type = ro, define=yes;
}