MEMORY {
    HEADER: start = $0800, size = 8, file = %O;             # Explicit start position
    ROM:    start = $0808, size = $7FF8, file = %O;         # Start right after HEADER
}

SEGMENTS {
    HEADER: load = HEADER, type = ro, define = yes;
    CODE:   load = ROM, type = ro;
}
